---
description: Зупинити dev-сервер поточної live-сесії
---

Run `bash "${CLAUDE_PLUGIN_ROOT}/bin/live.sh" stop` and report the result in one
line. Nothing else — no cleanup, no summary of the session.
